// Program sprawdzający warunki
int glowny_licznik = 0;
int limit = 10;

while (glowny_licznik < limit) {
    if (glowny_licznik == 5) {
        string wiadomosc = "Osiagnieto polowe!";
    } else {
        glowny_licznik = glowny_licznik + 1;
    }
}

return glowny_licznik;

$#@ // Test nierozpoznanych znaków na koniec